var React = require("react");

var WeatherMessage = React.createClass({
    render: function () {
        return (
            <h2>It is 40 in Makati</h2>
        );
    }
});

module.exports = WeatherMessage;